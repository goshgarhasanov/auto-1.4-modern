<?php
if ( $row['level'] <= 6 && $wtime == 0 )
{
echo "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";
echo "<!DOCTYPE wml PUBLIC \"-//WAPFORUM//DTD WML 1.2//EN\" \"http://www.wapforum.org/DTD/wml12.dtd\">\n";
echo "<wml>\n";
echo "<head><meta http-equiv=\"Cache-Control\" content=\"no-cache\" forua=\"true\"/></head>\n";
echo "<card id=\"xeta\" title=\"Telesme\">\n";
echo "<p align=\"center\">\n";
echo $fsize1;
echo "Xaric Edilecek leqebin vaxtini se&#231;memisiz nece deqiqelik Xaric edim?<br/>****<br/>\n";
if ( $rm != "" )
{
echo "<a href=\"chat.php?id={$id}&amp;ps={$ps}&amp;rm={$rm}&amp;ref={$ref}\">&#199;ata Qay&#305;t</a>\n";
}
else
{
echo "<b><a href=\"admin.php?id={$id}&amp;ps={$ps}&amp;ref={$ref}\">{$pname}</a></b>\n";
}
echo "<br/><a href=\"enter.php?id={$id}&amp;ps={$ps}&amp;ref={$ref}\">Dehliz</a>\n";
echo $fsize2;
echo "</p>\n";
echo "</card>\n";
echo "</wml>\n";
mysql_close( $link );
exit( );
}
$otime = time( );
$vtme = $inf['kik'];
if ( $row['level'] <= 7 && $otime < $vtme )
{
$tkick = $vtme - time( );
if ( $tkick < 60 && 0 < $tkick )
{
$var = "saniyyelik";
}
else if ( $tkick < 3600 && 60 < $tkick )
{
$new = $tkick;
$tkick = $new / 60;
$var = "deqiqelik";
}
else if ( $tkick < 86400 && 3600 < $tkick )
{
$new = $tkick;
$tkick = $new / 3600;
$var = "saatl&#305;q";
}
else if ( 86400 < $tkick )
{
$new = $tkick;
$tkick = $new / 86400;
$var = "g&#252;nl&#252;k";
}
$tkick = round( $tkick, 0 );
echo "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";
echo "<!DOCTYPE wml PUBLIC \"-//WAPFORUM//DTD WML 1.2//EN\" \"http://www.wapforum.org/DTD/wml12.dtd\">\n";
echo "<wml>\n";
echo "<head><meta http-equiv=\"Cache-Control\" content=\"no-cache\" forua=\"true\"/></head>\n";
echo "<card id=\"twk\" title=\"twk\">\n";
echo "<p align=\"center\">\n";
echo $fsize1;
if ( $xare == $user )
{
echo "{$pnik} leqebli istifadecini siz ujey {$tkick} {$var}  xaric edibsiz...<br/>****<br/>\n";
}
else
{
echo "{$pnik} leqebli istifadecini sizden evvel <u>{$xare}</u>, {$tkick} {$var}  xaric edib...<br/>****<br/>\n";
}
if ( $rm != "" )
{
echo "<a href=\"chat.php?id={$id}&amp;ps={$ps}&amp;rm={$rm}&amp;ref={$ref}\">&#199;ata Qay&#305;t</a>\n";
}
else
{
echo "<b><a href=\"admin.php?id={$id}&amp;ps={$ps}&amp;ref={$ref}\">{$pname}</a></b>\n";
}
echo "<br/><a href=\"enter.php?id={$id}&amp;ps={$ps}&amp;ref={$ref}\">Dehliz</a>\n";
echo $fsize2;
echo "</p>\n";
echo "</card>\n";
echo "</wml>\n";
mysql_close( $link );
exit( );
}
$soft = $inf['user_soft'];
$pid = $inf['id'];
echo "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n";
echo "<!DOCTYPE wml PUBLIC \"-//WAPFORUM//DTD WML 1.2//EN\" \"http://www.wapforum.org/DTD/wml12.dtd\">\n";
echo "<wml>\n";
echo "<head><meta http-equiv=\"Cache-Control\" content=\"no-cache\" forua=\"true\"/></head>\n";
echo "<card id=\"okey\" title=\"Okey\">\n";
echo "<p align=\"center\">\n";
echo $fsize1;
if ( $u_level < $row['level'] )
{
$totime = $wtime;
$wtime = $wtime * 60 + time( );
if ( $totime == 0 )
{
echo "\"<b>{$pnik}</b>\" &#199;ata Qaytar&#305;ld&#305;!\n";
mysql_query( "UPDATE users SET kik = '".time( )."' WHERE id = '".$pid."'" );
if ( $whykik != "" )
{
$whyki = "Qeyd: (<u>{$whykik}</u>)";
}
@$fi = @fopen( "file/control/4.dat", "a+" );
$data = date( "d.m.y [H:i]", mktime( date( "H" ) + $xsat ) );
$lst = "".base64_encode( "{$user} - \"<b>{$pnik}</b>\" [<u>{$otaqadi}</u>] {$whyki}  {$data}" )."\n";
@fwrite( @$fi, @"{$lst}" );
@fflush( @$fi );
@fclose( @$fi );
if ( $otaq < 10 )
{
$online = time( ) + $vaxt;
mysql_query( "UPDATE `users` SET `time` = '".$online."', `room` = '".$rm."' WHERE `id` = '7';" );
}
}
else
{
echo "\"<b>{$pnik}</b>\" Xaric edildi!!!\n";
mysql_query( "UPDATE users SET kik = '".$wtime."', whokik = '".$user."', whykik = '".$whykik."' WHERE id = '".$pid."'" );
if ( $whykik != "" )
{
$whykik = " (Sebeb: <u>{$whykik}</u>.)";
}
$tkick = $wtime - time( );
if ( $tkick < 3600 && 59 < $tkick )
{
$new = $tkick;
$tkick = $new / 60;
$var = "deqiqelik";
}
else if ( $tkick < 86400 && 3599 < $tkick )
{
$new = $tkick;
$tkick = $new / 3600;
$var = "saatl&#305;q";
}
else if ( 86399 < $tkick )
{
$new = $tkick;
$tkick = $new / 86400;
$var = "g&#252;nl&#252;k";
}
$tkick = round( $tkick, 0 );
@$fi = @fopen( "file/control/3.dat", "a+" );
$data = date( "d.m.y [H:i]", mktime( date( "H" ) + $xsat ) );
$lst = "".base64_encode( "{$user} - [<b>{$pnik}</b>] {$whykik} ({$tkick} {$var}.) {$data}" )."\n";
@fwrite( @$fi, @"{$lst}" );
@fflush( @$fi );
@fclose( @$fi );
if ( $otaq < 10 )
{
$i = 0;
while ( $i <= 9 )
{
$st = time( );
$today = date( "H:i", mktime( date( "H" ) + $xsat ) );
$mes = "<b>{$user_admin}</b> - \"<u>{$pnik}</u>\" <b>leqebli istifade&#231;ini {$tkick} {$var} chatdan xaric etdi.</b>{$whykik}";
$rnd = rand( 0, 99999999 );
mysql_query( "Insert into room{$i} set klu4= '".$rnd."', time='".$today."', who='".$xeberci."', message='".$mes."', id='".$st."', towhom='', hid='0', usid='7'" );
$online = time( ) + $vaxt;
mysql_query( "UPDATE `users` SET `time` = '".$online."', `room` = '".$otaq."' WHERE `id` = '7';" );
++$i;
}
mysql_query( "UPDATE `conf` SET ipp = '".$ip."', soft = '".$soft."';" );
}
}
}
else
{
$levelselect = @mysql_query( @"Select name from levels where level='".@$u_level."'" );
$levels = @mysql_fetch_array( @$levelselect );
$levname = $levels['name'];
echo " \"<b>".$levname."</b>\" Xaric etmeye ixtiyar&#305;n&#305;z yoxdu)))\n";
}
echo "<br/>****<br/>\n";
if ( $rm != "" )
{
echo "<a href=\"chat.php?id={$id}&amp;ps={$ps}&amp;rm={$rm}&amp;ref={$ref}\">&#199;ata Qay&#305;t</a>\n";
}
else
{
echo "<a href=\"admin.php?id={$id}&amp;ps={$ps}&amp;ref={$ref}\">{$pname}</a> \n";
}
echo "<br/><a href=\"enter.php?id={$id}&amp;ps={$ps}&amp;ref={$ref}\">Dehliz</a>\n";
echo $fsize2;
echo "</p>\n";
echo "</card>\n";
echo "</wml>\n";
?>
